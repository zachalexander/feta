import { LikeButtonAlreadyPipe } from './like-button-already.pipe';

describe('LikeButtonAlreadyPipe', () => {
  it('create an instance', () => {
    const pipe = new LikeButtonAlreadyPipe();
    expect(pipe).toBeTruthy();
  });
});
