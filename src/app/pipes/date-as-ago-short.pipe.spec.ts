import { DateAsAgoShortPipe } from './date-as-ago-short.pipe';

describe('DateAsAgoShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAsAgoShortPipe();
    expect(pipe).toBeTruthy();
  });
});
